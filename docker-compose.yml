version: "3.3"
services:
  bookshelfhome:
    image: bookshelfhome
    container_name: bookshelfhome
    ports:
      - 8093:8093
    restart: unless-stopped
    volumes:
      - /var/jenkins_home/bookshelfdb:/app/prisma